{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; print(json.load(sys.stdin)['tool_input']['file_path'])\" | { read -r f; echo \"$f\" | grep -qE '\\.(h|hpp|cpp|c|cc)$' && clang-format -i \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Formatting C/C++..."
          }
        ]
      }
    ]
  }
}